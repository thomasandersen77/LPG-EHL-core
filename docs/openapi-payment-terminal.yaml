openapi: 3.0.3
info:
  title: Payment Terminal Mono Server API
  version: 1.0.0
  description: |
    REST API for Baxi/Nets payment terminal integration.

    This API provides a modern HTTP interface for payment terminal operations,
    replacing the legacy VB6 COM/ActiveX integration pattern.

    **Key Concepts:**
    - Single terminal, single in-flight operation
    - Terminal readiness is mandatory for financial operations
    - Operations are non-cancellable by HTTP disconnect
    - Idempotency support via clientRequestId

    **Response Field Casing:**
    - Responses use camelCase (success, operationId, errorCode)
    - Exception: /health already uses lowercase keys (camelCase)
    - Requests are case-insensitive (camelCase or PascalCase accepted)
    - POST endpoints require a JSON body (send `{}` for empty bodies) to avoid `411 Length Required`

  contact:
    name: Cloudberries
    url: https://cloudberries.no
  license:
    name: Proprietary

servers:
  - url: http://localhost:8080
    description: Local payment terminal server (default port, configurable via server.json)
  - url: http://localhost:18080
    description: Alternative port (as configured in production server.json)

tags:
  - name: Health
    description: Server health and readiness checks
  - name: Terminal
    description: Terminal lifecycle management (open/close)
  - name: Payments
    description: Financial operations (purchase, refund, cashback)
  - name: Administration
    description: Administrative operations (avstemming, reversal, reports)
  - name: Events
    description: Real-time event streaming (SSE)
  - name: Diagnostics
    description: Low-level diagnostic operations (disabled by default)

components:
  schemas:
    # Health
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          example: "ok"
          description: Overall health status (lowercase)
        timestamp:
          type: string
          format: date-time
          description: Server timestamp (lowercase)
        configLoaded:
          type: boolean
          description: Whether configuration loaded successfully (lowercase)

    # Terminal Status
    TerminalStatusResponse:
      type: object
      properties:
        vendorDllLoadable:
          type: boolean
          description: Whether vendor DLL is loadable
        terminalOpen:
          type: boolean
          description: Whether terminal is currently open
        terminalReady:
          type: boolean
          description: Whether terminal is ready for operations
        connectionState:
          type: string
          nullable: true
          description: Connection state (e.g. "Open", "Aborted", "None")
        lastError:
          type: string
          nullable: true
          description: Last error message (if any)
        terminalIdentity:
          type: object
          nullable: true
          description: Terminal identity information (available when open)
          properties:
            terminalID:
              type: string
              description: Terminal identifier

    # Simple Response (used by terminal open/close)
    SimpleResponse:
      type: object
      required:
        - success
      properties:
        success:
          type: boolean
          description: Operation success flag
        message:
          type: string
          nullable: true
          description: Success message
        error:
          type: string
          nullable: true
          description: Error message (if failed)

    # Operation Response (used by all financial/admin operations)
    OperationResponse:
      type: object
      required:
        - success
        - operationId
        - callResult
      properties:
        success:
          type: boolean
          description: |
            Overall success flag computed server-side.
            For financial operations: true when callResult==1 AND (localModeResult==0 OR responseCode=="00")
            For admin operations, success may be false even when localModeResult==0 (inspect response body).
        operationId:
          type: string
          description: Server-generated operation identifier
        startedAt:
          type: string
          format: date-time
          nullable: true
          description: Operation start timestamp
        completedAt:
          type: string
          format: date-time
          nullable: true
          description: Operation completion timestamp
        durationMs:
          type: integer
          format: int64
          nullable: true
          description: Operation duration in milliseconds

        # Vendor call result
        callResult:
          type: integer
          description: |
            Vendor DLL call result.
            1 = call started successfully
            Other values = call failed to start
        methodRejectCode:
          type: integer
          nullable: true
          description: Method reject code (vendor-specific)
        methodRejectInfo:
          type: string
          nullable: true
          description: Method reject info (vendor-specific)
        resultEventName:
          type: string
          nullable: true
          description: Terminal event name for result (e.g. "OnLocalMode")
        errorCode:
          type: string
          nullable: true
          enum:
            - terminal_busy
            - terminal_not_ready
            - operation_timeout
            - operation_rejected
            - vendor_call_failure
            - diagnostics_disabled
          description: Error code (if operation failed)
        error:
          type: string
          nullable: true
          description: Human-readable error message

        # Terminal outcome fields
        localModeResult:
          type: integer
          nullable: true
          description: |
            Terminal local mode result.
            0 = approved
            2 = rejected/cancelled
        responseCode:
          type: string
          nullable: true
          description: |
            Terminal response code.
            "00" = approved
            "Z1" = wrong PIN
            Empty = user cancel
        entryMode:
          type: string
          nullable: true
          description: Entry mode (e.g. "CHIP", "CONTACTLESS")
        entryModeCode:
          type: string
          nullable: true
          description: Entry mode code (e.g. "0"=CHIP, "2"=CONTACTLESS)
        localModeResultData:
          type: string
          nullable: true
          description: Raw semicolon-delimited result data
        rejectionSource:
          type: string
          nullable: true
          description: Rejection source code (e.g. "3" for PIN error)
        rejectionReason:
          type: string
          nullable: true
          description: Detailed rejection reason (e.g. "3:2:Z1" for wrong PIN)

        # Evidence/receipt data
        printTextRaw:
          type: string
          nullable: true
          description: Raw receipt text from terminal
        printTextSanitized:
          type: string
          nullable: true
          description: Sanitized receipt text (cleaned formatting)
        lastDisplayText:
          type: string
          nullable: true
          description: Last display prompt shown to customer

        # Terminal identification
        localModeFields:
          type: object
          nullable: true
          additionalProperties: true
          description: Terminal and merchant identifier fields

        # Server persistence references
        dbRowId:
          type: integer
          nullable: true
          description: Database row ID for this operation
        receiptFileId:
          type: string
          nullable: true
          description: Receipt file identifier

        # Report-specific fields (for Z/X reports, avstemming)
        reportFields:
          type: object
          nullable: true
          additionalProperties: true
          description: Parsed report fields (when available)

    # Purchase Request
    PurchaseRequest:
      type: object
      required:
        - amountMinor
      properties:
        amountMinor:
          type: integer
          description: Amount in minor units (øre for NOK)
          example: 12500
        operatorId:
          type: string
          default: "0000"
          description: Operator ID (typically "0000" to match VB6)
        currency:
          type: string
          default: "NOK"
          description: Currency code
        optionalData:
          type: string
          nullable: true
          description: Optional text for receipt (e.g. "LPG Autogas")
        clientRequestId:
          type: string
          nullable: true
          description: |
            Client-provided idempotency key.
            Recommended format: "fuelingSession:<session-id>"
        preAvstemming:
          $ref: '#/components/schemas/PreAvstemmingConfig'

    # Refund Request
    RefundRequest:
      type: object
      required:
        - amountMinor
      properties:
        amountMinor:
          type: integer
          description: Amount in minor units (øre for NOK)
        operatorId:
          type: string
          default: "0000"
        optionalData:
          type: string
          nullable: true
          description: Optional text for receipt (e.g. "Return LPG Autogas")
        clientRequestId:
          type: string
          nullable: true
          description: Client-provided idempotency key
        preAvstemming:
          $ref: '#/components/schemas/PreAvstemmingConfig'

    # Pre-avstemming Configuration
    PreAvstemmingConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
          description: Enable pre-avstemming before operation
        password:
          type: string
          default: "0000"
          description: Admin password for avstemming
        timeoutSeconds:
          type: integer
          nullable: true
          description: Timeout in seconds (optional)

    # Cashback Request
    CashbackRequest:
      type: object
      required:
        - purchaseMinor
        - cashbackMinor
      properties:
        purchaseMinor:
          type: integer
          description: Purchase amount in minor units (CORRECTED FIELD NAME)
        cashbackMinor:
          type: integer
          description: Cashback amount in minor units (CORRECTED FIELD NAME)
        currency:
          type: string
          default: "NOK"
          description: Currency code
        operatorId:
          type: string
          default: "4321"
          description: Operator ID (note different default than purchase/refund)
        optionalData:
          type: string
          nullable: true
          description: Optional text for receipt
        clientRequestId:
          type: string
          nullable: true
          description: Client-provided idempotency key

    # Admin Request (for operations requiring password)
    AdminRequest:
      type: object
      required:
        - password
      properties:
        password:
          type: string
          default: "0000"
          description: Admin password (typically "0000")

    # Admin Code Request (for generic admin codes)
    AdminCodeRequest:
      type: object
      required:
        - code
      properties:
        code:
          type: integer
          description: |
            Admin code in decimal.
            Examples:
            - 12593 (0x3131) = Empty printer buffer
            - 12598 (0x3136) = X-report
            - 12603 (0x313B) = Unknown/other
        password:
          type: string
          default: "0000"

    # Event Envelope (for SSE and polling)
    EventEnvelope:
      type: object
      required:
        - cursor
        - eventId
        - timestamp
        - eventType
      properties:
        cursor:
          type: integer
          format: int64
          description: Monotonic sequence number for event ordering
        eventId:
          type: string
          description: Event identifier (CORRECTED - was missing)
        operationId:
          type: string
          nullable: true
          description: Operation ID (if event is tied to an operation)
        timestamp:
          type: string
          format: date-time
          description: Event timestamp
        eventType:
          type: string
          description: Event type identifier
        payload:
          type: object
          additionalProperties: true
          description: Event-specific payload data

    # Diagnostics Response (lowercase keys, anonymous object)
    DiagnosticsResponse:
      type: object
      properties:
        success:
          type: boolean
          description: Operation success (lowercase)
        callResult:
          type: integer
          description: Vendor call result (lowercase)

    # Error Response
    ErrorResponse:
      type: object
      required:
        - error
        - errorCode
      properties:
        error:
          type: string
          description: Human-readable error message (CORRECTED FIELD NAME)
        errorCode:
          type: string
          description: Machine-readable error code
        operationId:
          type: string
          nullable: true
          description: Operation ID (if applicable)
        details:
          type: string
          nullable: true
          description: Additional error details as string (CORRECTED TYPE)

  responses:
    BadRequest:
      description: Bad request (invalid parameters)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Timeout:
      description: Operation timeout
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: "Terminal operation did not complete within timeout"
            errorCode: "operation_timeout"

    Conflict:
      description: Terminal busy (another operation in progress)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: "Another operation is currently in progress"
            errorCode: "terminal_busy"

    UnprocessableEntity:
      description: Operation rejected by terminal
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: "Terminal rejected the operation"
            errorCode: "operation_rejected"

    ServiceUnavailable:
      description: Terminal not ready
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: "Terminal is not ready for operations"
            errorCode: "terminal_not_ready"

    InternalServerError:
      description: Vendor call failure or server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: "Vendor DLL call failed"
            errorCode: "vendor_call_failure"

paths:
  # Health
  /health:
    get:
      tags:
        - Health
      summary: Health check
      description: |
        Verify that the server process is running.
        Returns lowercase keys (matches standard camelCase response casing).
      operationId: getHealth
      responses:
        '200':
          description: Server is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  # Terminal Management
  /v1/terminal/status:
    get:
      tags:
        - Terminal
      summary: Get terminal status
      description: |
        Check terminal readiness before attempting operations.
        Verify that:
        - vendorDllLoadable = true
        - terminalOpen = true
        - terminalReady = true

        Additional fields:
        - connectionState ("Open", "Aborted", "None")
        - terminalIdentity.terminalID (available when open)
      operationId: getTerminalStatus
      responses:
        '200':
          description: Terminal status retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TerminalStatusResponse'

  /v1/terminal/open:
    post:
      tags:
        - Terminal
      summary: Open terminal
      description: |
        Initialize and open the terminal connection.
        Call this if terminalOpen = false.
        Note: server requires a JSON body (send `{}`) or returns `411 Length Required`.
      operationId: openTerminal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Terminal opened (check success field)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleResponse'
        '500':
          description: Terminal open failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleResponse'

  /v1/terminal/close:
    post:
      tags:
        - Terminal
      summary: Close terminal
      description: |
        Close the terminal connection.
        Used for terminal restart: close → sleep → open.
        Note: server requires a JSON body (send `{}`) or returns `411 Length Required`.
      operationId: closeTerminal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Terminal closed (check success field)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleResponse'
        '500':
          description: Terminal close failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleResponse'

  # Financial Operations
  /v1/payments/purchase:
    post:
      tags:
        - Payments
      summary: Purchase (card payment)
      description: |
        Perform a card purchase operation.

        VB6 equivalent: `TransferAmount_V2("0000", &H30, amountMinor, ...)`

        **Typical flow:**
        1. Call this endpoint with amount
        2. Customer interacts with terminal (insert card, enter PIN)
        3. Response indicates success or failure
        4. If success=true, proceed with dispenser operations
        5. If dispenser fails after approval, call /v1/admin/reversal

        **Idempotency:**
        Send clientRequestId to enable idempotent retries.

        **Observed behavior:**
        - If the terminal is closed, the server may block for ~180 seconds and return `408 operation_timeout`
          with `callResult=0` instead of an immediate `503 terminal_not_ready`.
      operationId: purchase
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseRequest'
      responses:
        '200':
          description: Purchase completed (check success field)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/payments/refund:
    post:
      tags:
        - Payments
      summary: Refund (return)
      description: |
        Perform a refund/return operation.

        VB6 equivalent: `TransferAmount_V2("0000", &H31, amountMinor, ...)`

        Despite VB6 calling this "cashback", it's actually a refund operation.
      operationId: refund
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefundRequest'
      responses:
        '200':
          description: Refund completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /v1/payments/cashback:
    post:
      tags:
        - Payments
      summary: Purchase with cashback
      description: |
        Perform a combined purchase + cashback operation.
        Only use if your application explicitly needs this composite flow.
      operationId: cashback
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CashbackRequest'
      responses:
        '200':
          description: Cashback completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          $ref: '#/components/responses/InternalServerError'

  # Administrative Operations
  /v1/admin/avstemming:
    post:
      tags:
        - Administration
      summary: Avstemming (reconciliation)
      description: |
        Perform end-of-day reconciliation.

        VB6 equivalent: `Administration &H3130`
        Admin code: 0x3130
      operationId: avstemming
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Avstemming completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/cancel:
    post:
      tags:
        - Administration
      summary: Cancel current operation
      description: |
        Cancel the current terminal operation.

        VB6 equivalent: `Administration &H3132`
        Admin code: 0x3132
      operationId: cancelOperation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Cancel sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/reversal:
    post:
      tags:
        - Administration
      summary: Reversal of last transaction
      description: |
        Reverse the last successful transaction.

        VB6 equivalent: `Administration &H3134`
        Admin code: 0x3134

        **Important:** This is not reversible. VB6 shows warning:
        "Annuler siste korttransaksjon? Dette er ikke reversibelt!"

        **When to use:**
        - Payment succeeded but dispenser communication failed
        - Payment succeeded but delivery could not start
      operationId: reversal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Reversal completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/z-report:
    post:
      tags:
        - Administration
      summary: Z-report (end-of-day report)
      description: |
        Generate end-of-day Z-report.

        VB6 equivalent: `Administration &H3137`
        Admin code: 0x3137

        **Note:** VB6 appends additional data from its own database
        (technical refunds since last Z). This must be handled client-side.
      operationId: zReport
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Z-report completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/last-receipt:
    post:
      tags:
        - Administration
      summary: Print last receipt
      description: |
        Print/retrieve the last financial receipt.

        VB6 equivalent: `Administration &H313C`
        Admin code: 0x313C
      operationId: lastReceipt
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Last receipt retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/software:
    post:
      tags:
        - Administration
      summary: Software download
      description: |
        Download software update to terminal.

        VB6 equivalent: `Administration &H313E`
        Admin code: 0x313E

        **Note:** This is a long-running operation. May timeout.
      operationId: softwareDownload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Software download completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/dataset:
    post:
      tags:
        - Administration
      summary: Dataset download
      description: |
        Download dataset to terminal.

        VB6 equivalent: `Administration &H313F`
        Admin code: 0x313F

        **Note:** This is a long-running operation. May timeout.
      operationId: datasetDownload
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminRequest'
      responses:
        '200':
          description: Dataset download completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  /v1/admin/code:
    post:
      tags:
        - Administration
      summary: Generic admin code
      description: |
        Execute a generic admin code not covered by specific endpoints.

        **Common codes:**
        - 12593 (0x3131) = Empty printer buffer
        - 12598 (0x3136) = X-report
        - 12603 (0x313B) = Unknown/other (seen in VB6)
      operationId: genericAdminCode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminCodeRequest'
      responses:
        '200':
          description: Admin code executed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'
        '408':
          $ref: '#/components/responses/Timeout'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          description: Server may return 500 even when operation completed; inspect response body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OperationResponse'

  # Events
  /v1/events/stream:
    get:
      tags:
        - Events
      summary: Subscribe to event stream (SSE)
      description: |
        Subscribe to real-time events via Server-Sent Events (SSE).

        **Cursor strategy:**
        - First connect: `?since=0`
        - Reconnect: `?since=<last-cursor>` (persist last delivered cursor)

        **Event format:**
        - `event:` field = EventType
        - `data:` field = JSON EventEnvelope

        **Observed SSE behavior:**
        - Initial `event: connected` with `data: {"since":"<cursor>"}`
        - Keepalive comments every ~1 second (`: keepalive <ISO timestamp>`)
      operationId: streamEvents
      parameters:
        - name: since
          in: query
          description: Start from cursor (0 for beginning, or ISO timestamp)
          schema:
            type: string
            default: "0"
      responses:
        '200':
          description: SSE stream established
          content:
            text/event-stream:
              schema:
                type: string
                description: Server-Sent Events stream

  /v1/events:
    get:
      tags:
        - Events
      summary: Poll for events
      description: |
        Poll for events since a given cursor (alternative to SSE).

        Returns array of EventEnvelope objects.
      operationId: pollEvents
      parameters:
        - name: since
          in: query
          description: Start from cursor (0 for beginning, or ISO timestamp)
          schema:
            type: string
            default: "0"
      responses:
        '200':
          description: Events retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EventEnvelope'

  # Diagnostics (disabled by default)
  /v1/diag/schema:
    get:
      tags:
        - Diagnostics
      summary: Get terminal schema
      description: |
        Retrieve terminal schema information (for investigation).

        **Note:** Only available when diagnostics are enabled.
      operationId: getDiagSchema
      responses:
        '200':
          description: Schema retrieved
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '403':
          description: Diagnostics disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Diagnostics endpoints are disabled"
                errorCode: "diagnostics_disabled"
                operationId: null
                details: null

  /v1/diag/sendjson:
    post:
      tags:
        - Diagnostics
      summary: Send raw JSON to terminal
      description: Send raw JSON command to terminal (for debugging)
      operationId: sendDiagJson
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - json
              properties:
                json:
                  type: string
                  description: JSON string to send to terminal
      responses:
        '200':
          description: JSON sent (lowercase response keys)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiagnosticsResponse'
        '403':
          description: Diagnostics disabled

  /v1/diag/sendtld:
    post:
      tags:
        - Diagnostics
      summary: Send TLD data to terminal
      description: Send raw TLD (Tag-Length-Data) to terminal
      operationId: sendDiagTld
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tldType
                - tldData
              properties:
                tldType:
                  type: string
                  description: TLD type identifier
                tldData:
                  type: string
                  format: base64
                  description: Base64-encoded TLD data
      responses:
        '200':
          description: TLD sent (lowercase response keys)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiagnosticsResponse'
        '403':
          description: Diagnostics disabled

  /v1/diag/confirm:
    post:
      tags:
        - Diagnostics
      summary: Confirm diagnostic operation
      description: Confirm or deny a pending diagnostic operation
      operationId: confirmDiag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - allow
              properties:
                id:
                  type: integer
                  description: Operation ID to confirm
                allow:
                  type: boolean
                  description: Allow (true) or deny (false)
      responses:
        '200':
          description: Confirmation processed (lowercase response keys)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiagnosticsResponse'
        '403':
          description: Diagnostics disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: "Diagnostics endpoints are disabled"
                errorCode: "diagnostics_disabled"
                operationId: null
                details: null
