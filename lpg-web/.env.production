# Production build: use relative URLs (API served from same origin)
# When deploying to e.g. http://10.0.0.169:8080, API calls go to same host.
# Leave empty - do NOT use localhost here.
VITE_API_URL=
VITE_API_BASE_URL=
VITE_EMULATOR_BASE_URL=
VITE_EMULATOR_URL=
