<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-drop-old-id-column" author="system">
        <preConditions onFail="MARK_RAN">
            <columnExists tableName="transactions" columnName="id"/>
        </preConditions>
        
        <comment>Drop old id column and use transaction_id as primary key</comment>
        
        <!-- Drop existing primary key on 'id' column with CASCADE to handle foreign keys -->
        <sql dbms="postgresql">ALTER TABLE transactions DROP CONSTRAINT IF EXISTS transactions_pkey CASCADE</sql>
        <sql dbms="h2">ALTER TABLE transactions DROP CONSTRAINT IF EXISTS transactions_pkey</sql>
        
        <!-- Drop the old 'id' column with CASCADE -->
        <sql dbms="postgresql">ALTER TABLE transactions DROP COLUMN IF EXISTS id CASCADE</sql>
        <sql dbms="h2">ALTER TABLE transactions DROP COLUMN IF EXISTS id</sql>
        
        <!-- Add primary key constraint to transaction_id -->
        <addPrimaryKey tableName="transactions" columnNames="transaction_id" constraintName="transactions_pkey"/>
        
        <!-- Recreate foreign key constraints that were dropped with CASCADE -->
        <sql>
            ALTER TABLE protocol_events
            ADD CONSTRAINT fk_protocol_events_transaction
            FOREIGN KEY (transaction_id) REFERENCES transactions(transaction_id);
            
            ALTER TABLE credit_accounts
            ADD CONSTRAINT fk_credit_account_last_transaction
            FOREIGN KEY (last_transaction_id) REFERENCES transactions(transaction_id);
        </sql>
    </changeSet>

</databaseChangeLog>
