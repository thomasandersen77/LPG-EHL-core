package no.cloudberries.norgesgass.baxi.config

/**
 * Minimal configuration used by the `baxi-kotlin` client.
 *
 * This is a compile-time stub to allow `lpg-ehl-service` to build.
 * Replace with the real implementation when the actual baxi-kotlin library is available.
 */
data class BaxiIniConfig(
    val hostIpAddress: String,
    val hostPort: Int,
    val vendorInfoExtended: String?,
    val socketListenerEnabled: Boolean,
    val socketListenerPort: Int?,
)

